# Backend-only deploy: force Python even though package.json exists for the Vite frontend.
providers = ["python"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python3 -m uvicorn src.api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
